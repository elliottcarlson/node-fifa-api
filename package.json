{
  "name": "fifa-api",
  "version": "1.0.0",
  "description": "FIFA Match Event API",
  "main": "lib/index.js",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.75.0",
    "colors": "^1.3.0",
    "debug": "^3.1.0",
    "event-emitter-es6": "^1.1.5",
    "node-emoji": "^1.8.1",
    "request": "^2.87.0",
    "request-promise": "^4.2.2"
  },
  "scripts": {
    "build": "babel src/ -d lib/",
    "prepublish": "yarn run build",
    "test": "yarn run prepublish && babel-node test.js"
  }
}
